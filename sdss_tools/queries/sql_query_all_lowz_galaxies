SELECT
   s.bestObjID,s.ra,s.dec,s.z,
   s.velDisp,s.velDispErr, s.plate, s.mjd, s.fiberid
FROM SpecObj AS s
WHERE 
   s.class='GALAXY'
   AND s.survey='boss'
   AND s.BOSS_TARGET1 % 2 = 1
   AND s.SCIENCEPRIMARY = 1
   AND s.ZWARNING_NOQSO = 0
