SELECT
   s.bestObjID,s.ra,s.dec,s.z,s.class, s.z,
   s.velDisp,s.velDispErr
FROM SpecObj AS s
WHERE 
   s.class='GALAXY'
   AND s.survey='boss'
   AND s.BOSS_TARGET1 % 2 = 1
   AND s.SCIENCEPRIMARY = 1
   AND s.ZWARNING_NOQSO = 0
